NAME
    all_directions - returns all exit directions
 
SYNOPSIS
    #include <acme.h>
    static inherit AcmeRoom;
    #include AcmeRoomInc

    string *all_directions( object room );
 
DESCRIPTION
    Returns an array of all the exit directions in room.
 
SEE_ALSO
    visible_directions(A), visible_exits(A), invisible_directions(A), 
    invisible_exits(A), all_exits(A), get_exit(A)
 
LAST MODIFIED                               
    980102 Devo
