NAME
    dequeue - remove an element from the queue
 
SYNOPSIS
    #include <acme.h>
    static inherit AcmeQueue;
    #include AcmeQueueInc

    mixed dequeue( QueueHdl hdl ) ;
 
DESCRIPTION
    Removes an element from the queue, and returns the element 
    removed, or QUEUE_EMPTY if the queue is empty.
 
SEE_ALSO
    new(A), empty(A), enqueue(A), front(A)
 
LAST MODIFIED                               
    980102 Devo
