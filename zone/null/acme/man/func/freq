NAME
    freq - map the frequency of sample values
 
SYNOPSIS
    #include <acme.h>
    static inherit AcmeStat;
    #include AcmeStatInc
 
    mapping freq( float *sample );
 
DESCRIPTION
    Returns a mapping of all the values in sample mapped to how many 
    times they occured in sample.
 
EXAMPLE
    freq( ({ 1, 3, 3 }) -> ([ 1 : 1, 3 : 2 ])
 
SEE_ALSO
    rel_freq(A), cum_rel_freq(A)
 
LAST MODIFIED                               
    980502 Devo
