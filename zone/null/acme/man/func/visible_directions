NAME
    visible directions - return all visible exit directions
 
SYNOPSIS
    #include <acme.h>
    static inherit AcmeRoom;
    #include AcmeRoomInc

    string *visible_directions( object room );
 
DESCRIPTION
    Returns an array of all the visible exit directions in room.
 
SEE_ALSO
    visible_exits(A), invisible_directions(A), invisible_exits(A), 
    all_directions(A), all_exits(A), get_exit(A)
 
LAST MODIFIED                               
    980102 Devo
