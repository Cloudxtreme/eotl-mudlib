NAME
    to_hex - convert a decimal int to a string of hex digits
 
SYNOPSIS
    #include <acme.h>
    static inherit AcmeMath;
    #include AcmeMathInc

    string to_hex( int num );
 
DESCRIPTION
    Returns string in hex representing an integer value.
 
EXAMPLE
    to_hex(16) -> "10"
 
SEE_ALSO
    to_string(E), from_hex(A), to_octal(A), to_binary(A)
 
LAST MODIFIED                               
    980102 Devo
